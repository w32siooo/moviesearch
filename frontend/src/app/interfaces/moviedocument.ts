export interface MovieDocument{
    id : string,
    version : number,
    tconst : string,
    primaryTitle : string,
    originalTitle : string,
    startYear : number,
    endYear : number,
    runtimeMinutes : number,
    genres : string[]
}

